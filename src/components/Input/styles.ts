import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  input: {
    width: '100%',
    height: 54,
    borderRadius: 6,
    padding: 16,
    color: '#FFFFFF',
    backgroundColor: '#262626',
  },
});

export { styles }
